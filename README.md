ThingsView
==========

Basic Cocoa project that imports tasks from Cultured Code's Things.app and displays them in a standard table view. The data can be filtered by list, priority, tags, project/area and delegate. The data can also be exported to CSV.